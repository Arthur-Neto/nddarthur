export enum NDDBreadcrumbSizes {
    border = 3,
    padding_left = 5,
    padding_right = 25,
    options_menu_width= 38,
    base_width = 12,
}

export enum NDDBreadcrumbSelectors {
    content = '.ndd-ng-breadcrumb',
    options_selector = 'breadcrumbOptions',
}
