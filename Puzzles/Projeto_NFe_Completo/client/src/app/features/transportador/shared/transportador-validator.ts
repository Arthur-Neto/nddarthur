import { AbstractControl, ValidationErrors } from '@angular/forms';

export class TransportadorValidators {
  public static checkTransportadorDates(control: AbstractControl): ValidationErrors | null {
    return null;
  }
}
