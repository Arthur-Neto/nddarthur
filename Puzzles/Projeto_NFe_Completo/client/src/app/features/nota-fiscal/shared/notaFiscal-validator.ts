import { AbstractControl, ValidationErrors } from '@angular/forms';

export class NotaFiscalValidators {
  public static checkNotaFiscalDates(control: AbstractControl): ValidationErrors | null {
    return null;
  }
}
