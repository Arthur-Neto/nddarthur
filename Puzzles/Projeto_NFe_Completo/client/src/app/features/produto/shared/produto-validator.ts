import { AbstractControl, ValidationErrors } from '@angular/forms';

export class ProdutoValidators {
  public static checkProdutoDates(control: AbstractControl): ValidationErrors | null {
    return null;
  }
}
